info:
  name: status
  type: http
  seq: 1

http:
  method: GET
  url: http://{{api_url}}/api/core/system/status
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
