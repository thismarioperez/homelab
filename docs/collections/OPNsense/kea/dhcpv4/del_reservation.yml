info:
  name: del_reservation
  type: http
  seq: 8

http:
  method: POST
  url: http://{{api_url}}/api/kea/dhcpv4/del_reservation/$uuid
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
