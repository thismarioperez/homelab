info:
  name: upload_reservations
  type: http
  seq: 4

http:
  method: POST
  url: http://{{api_url}}/api/kea/dhcpv4/upload_reservations
  # payload is base64-encoded reservation import data
  body:
    type: json
    data: |
      {"payload": ""}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
