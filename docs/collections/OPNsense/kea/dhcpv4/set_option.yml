info:
  name: set_option
  type: http
  seq: 17

http:
  method: POST
  url: http://{{api_url}}/api/kea/dhcpv4/set_option/$uuid
  body:
    type: json
    data: |
      {
        "option": {
          "code": "6",
          "encoding": "hex",
          "data": "c0a80001",
          "description": "domain-name-servers"
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
