info:
  name: get_subnet
  type: http
  seq: 11

http:
  method: GET
  url: http://{{api_url}}/api/kea/dhcpv4/get_subnet/$uuid
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
