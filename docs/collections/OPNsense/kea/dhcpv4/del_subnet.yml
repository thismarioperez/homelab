info:
  name: del_subnet
  type: http
  seq: 13

http:
  method: POST
  url: http://{{api_url}}/api/kea/dhcpv4/del_subnet/$uuid
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
