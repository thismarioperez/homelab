info:
  name: get_peer
  type: http
  seq: 21

http:
  method: GET
  url: http://{{api_url}}/api/kea/dhcpv4/get_peer/$uuid
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
