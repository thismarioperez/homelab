info:
  name: set_subnet
  type: http
  seq: 12

http:
  method: POST
  url: http://{{api_url}}/api/kea/dhcpv4/set_subnet/$uuid
  body:
    type: json
    data: |
      {
        "subnet4": {
          "subnet": "192.168.10.0/24",
          "description": "example subnet",
          "valid_lifetime": "4000",
          "pools": "192.168.10.100-192.168.10.200"
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
