info:
  name: get_reservation
  type: http
  seq: 6

http:
  method: GET
  url: http://{{api_url}}/api/kea/dhcpv4/get_reservation/$uuid
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
