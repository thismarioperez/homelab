info:
  name: status
  type: http
  seq: 1

http:
  method: POST
  url: http://{{api_url}}/api/kea/service/status
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
