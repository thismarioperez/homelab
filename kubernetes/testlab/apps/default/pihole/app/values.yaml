# Throwaway instance for testing storage — not used for real DNS.
# No ingress/HTTPRoute; UI reachable at <node-ip>:30980.
service:
  main:
    type: NodePort
    ports:
      main:
        nodePort: 30980
persistence:
  config:
    size: 4Gi
  dnsmasq:
    size: 1Gi
